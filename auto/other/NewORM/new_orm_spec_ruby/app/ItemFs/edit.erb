<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @itemfs.a %></h1>
    <a href="<%= url_for :action => :show, :id => @itemfs.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @itemfs.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @itemfs.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="itemfs[a]" class="fieldLabel">A</label>
            <input type="text" id="itemfs[a]" name="itemfs[a]" value="<%= @itemfs.a %>" <%= placeholder( "A" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>

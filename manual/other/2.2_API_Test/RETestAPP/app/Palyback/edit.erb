<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @palyback.name %></h1>
    <a href="<%= url_for :action => :show, :id => @palyback.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @palyback.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @palyback.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="palyback[name]" class="fieldLabel">Name</label>
            <input type="text" id="palyback[name]" name="palyback[name]" value="<%= @palyback.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>

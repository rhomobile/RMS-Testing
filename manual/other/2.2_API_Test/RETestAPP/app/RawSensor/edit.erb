<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @rawsensor.name %></h1>
    <a href="<%= url_for :action => :show, :id => @rawsensor.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @rawsensor.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @rawsensor.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="rawsensor[name]" class="fieldLabel">Name</label>
            <input type="text" id="rawsensor[name]" name="rawsensor[name]" value="<%= @rawsensor.name %>" <%= placeholder( "Name" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>

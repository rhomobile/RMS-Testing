<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @cardreadermodule.CardReaderModel %></h1>
    <a href="<%= url_for :action => :show, :id => @cardreadermodule.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @cardreadermodule.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @cardreadermodule.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="cardreadermodule[CardReaderModel]" class="fieldLabel">Cardreadermodel</label>
            <input type="text" id="cardreadermodule[CardReaderModel]" name="cardreadermodule[CardReaderModel]" value="<%= @cardreadermodule.CardReaderModel %>" <%= placeholder( "Cardreadermodel" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
